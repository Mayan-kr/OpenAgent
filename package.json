{
  "name": "openagent",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "extension"
  ],
  "devDependencies": {
    "eslint": "^9.18.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.3"
  },
  "scripts": {
    "build": "npm run build --workspace @openagent/extension",
    "dev": "npm run dev --workspace @openagent/extension",
    "lint": "npm run lint --workspace @openagent/extension",
    "format:check": "prettier --check .",
    "test": "npm run test --workspace @openagent/extension"
  }
}
